<view class="nav-Min {{navshow?'active':''}}">
  <view class="nav-list">
    <view class="nav-li">
      <navigator url="url"></navigator>
    </view>
    <view class="nav-li">
      <view class="nav-li gridXt" data-key="index" bindtap="linkto">
        <text class="iconfont icon-homefill"></text>
        首页
      </view>
    </view>
    <block wx:for="{{setting.navList}}">
      <view class="nav-li gridXt" data-key="{{item.key}}" bindtap="linkto">
        {{item.title}}
      </view>
    </block>
  </view>
</view>

